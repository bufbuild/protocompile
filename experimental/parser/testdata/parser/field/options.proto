syntax = "proto3";

package test;

message M {
    M foo = 1 [foo = bar];
    M bar = 2 [];
    M baz = 3 [
        foo = bar,
        bang = 5
    ];
    
    M bad = 4 [foo: {bar: baz}];
    M bad2 = 5 [foo = {bar = baz}];
}