edition = "2023";

package buf.test;

message M {
    string x1 = 1 [features.message_encoding = DELIMITED];
    M x2 = 2 [features.message_encoding = DELIMITED];
    repeated M x3 = 3 [features.message_encoding = DELIMITED];
    map<int32, M> x4 = 4 [features.message_encoding = DELIMITED];
}
